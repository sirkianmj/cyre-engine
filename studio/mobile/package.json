{
  "name": "@cyre/studio-mobile",
  "version": "1.0.4",
  "private": true,
  "description": "Capacitor Android host for CYRE Studio",
  "scripts": {
    "sync": "capacitor sync android",
    "add:android": "capacitor add android",
    "open:android": "capacitor open android",
    "build:android": "capacitor sync android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  }
}
